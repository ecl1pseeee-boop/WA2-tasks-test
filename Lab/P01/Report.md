# Практика 1. Обзор системы и технический долг — отчёт

## 1. Как поднимали

При первом знакомстве со стартовым репозиторием разворачивание проводителось несколько раз, хотя по методичке ожидалось «почти работает»:

1. Первый запуск `docker compose up -d` в Docker Desktop упал с ошибкой снапшоттера
   (`failed call to UtimesNanoAt ... no such file or directory`). Это проблема окружения, 
   не проекта, уже при повторном запуске все прошло нормально.
2. В логах `php` обнаружился `Fatal error: Uncaught Error: Failed opening required
   '/var/www/html/vendor/autoload.php'` —
   `vendor/` в образе не было, а `composer install` при сборке не запускался.
3. Далее `file_get_contents(/var/www/html/.env): Failed to open stream` при
   `php artisan key:generate` — файла `.env` не было вообще, только `.env.example`.
4. В `.env.example` был прописан `DB_CONNECTION=sqlite`, при этом `docker-compose.yml`
   поднимает отдельный контейнер `mysql` и прокидывает в `php` переменные для MySQL —
   драйвер БД нужно было переключать в ручную ни в `.env`, ни в `docker-compose.yml`.


## 2. Проверка функций

Скрипт: `smoke.sh`. Полный вывод последнего успешного прогона:

```
=== Открытие страниц ===
OK   лента
OK   страница входа (200)
OK   страница регистрации (200)
OK   список пользователей (200)
OK   REST-API документация (200)
=== Ограничение: база MySQL отвечает ===
OK   MySQL отвечает
=== Регистрация ===
OK   регистрация (smoke_1788457141_1199@example.test)
=== Выход и вход паролем ===
OK   вход паролем (smoke_1788457141_1199@example.test)
=== Вход через GitHub (функция + ограничение заказчика) ===
OK   вход через GitHub (редирект на GitHub OAuth)
=== Подача объявления ===
OK   подача объявления
=== Поиск ===
OK   поиск
=== Просмотр объявления ===
OK   просмотр объявления
=== Комментарий ===
OK   комментарий

ИТОГ: всё зелёное
код возврата: 0
```

Что проверяет каждая проверка:

| Проверка | Как |
|---|---|
| лента | `GET /`, код 200 + подстрока «Лента объявлений» в теле |
| страница входа / регистрации / список пользователей / REST-API документация | `GET`, ожидается код 200 |
| MySQL отвечает | `docker compose exec mysql mysqladmin ping -h localhost` внутри контейнера |
| регистрация | `POST /register` с CSRF-токеном и данными формы, затем `GET /ads/create` должен отдать 200 (сессия залогинена) |
| вход паролем | `POST /logout`, затем `POST /login` тем же email/паролем, снова проверка `GET /ads/create` → 200 |
| вход через GitHub | `GET /auth/github`, ожидается 302 с редиректом на `github.com` |
| подача объявления | `POST /ads` с данными формы, затем проверка, что созданное объявление видно на своей странице |
| поиск | `GET /?q=<заголовок объявления>`, заголовок должен быть в теле ответа |
| просмотр объявления | `GET /ads/{id}`, описание объявления должно быть в теле |
| комментарий | `POST /ads/{id}/comments`, затем текст комментария должен появиться на странице объявления |

## 3. Замеры

### Время ответа (три прогона, секунды)

| Страница | Прогон 1 | Прогон 2 | Прогон 3 | Максимум |
|---|---|---|---|---|
| `/` | 0.824353 | 0.611900 | 0.621669 | **0.824353** |
| `/login` | 0.326818 | 0.319087 | 0.319319 | **0.326818** |
| `/register` | 0.317028 | 0.317410 | 0.319183 | **0.319183** |
| `/ads/1` | 0.323471 | 0.318925 | 0.323780 | **0.323780** |
| `/search?q=test` | 0.325997 | 0.319250 | 0.308521 | **0.325997** |

Требование заказчика — ≤ 3 с. Максимум по всем страницам — **0,63 с**, запас почти пятикратный.

### Ресурсы

`docker stats --no-stream`:

| Контейнер | CPU % | Память |
|---|---|---|
| nginx | 0.00% | 9.98 MiB |
| php | 0.00% | 93.4 MiB |
| api | 0.12% | 43.3 MiB |
| redis | 0.30% | 7.04 MiB |
| mysql | 0.45% | 380.2 MiB |
| **Итого** | — | **≈ 534 MiB (0,52 ГБ)** |

Бюджет — 3 из 4 ГБ. Занято меньше 0,2.

`df -h .` на хосте: 3.0 ГБ занято из 1007 ГБ.
`docker system df`: тома — 729 MB; образы — 18,26 ГБ, но это
накопленный кэш многократных пересборок за время разработки, а не то, что займёт одно
чистое развёртывание. 

По памяти и времени ответа укладываемся в рамки заказчика с большим запасом.
По диску — данные укладываются уверенно. Общий вес образов на чистом сервере не
измерен точно и это стоит сделать отдельно перед реальным запуском у Виктора.

## 4. Карта

Обе схемы — в `docs/architecture.md`.

**Проверка на человеке не выполнена** Это единственный пункт, который ещё
не сделан. Нужен сосед по группе, не писавший код, и вопрос «где обрабатывается подача
объявления», замер времени. Статус в `docs/requirements.md` оставлен как `?` осознанно, 
не подгонялась под готово.

## 5. Решения

Пять записей — `docs/adr/0001…0005.md`:

1. **Два сервиса (PHP + Python) вместо одного**.
2. **Одна общая MySQL на оба сервиса**.
3. **Вход через сессии + GitHub OAuth, без JWT**.
4. **Docker Compose на одном хосте вместо оркестратора**.
5. **Узкое место — единственный MySQL**.

## 6. Учение 1 (чистая машина)
Замеры были сделаны с использованием секундомера на другом устройстве.

| Прогон | Время | Итог |
|---|---|---|
| 1 | 1:26,42 | упал — `Fatal error: ... vendor/autoload.php` (не найден) |
| 2 | 1:09,95 | успешно, все сервисы поднялись |

Между прогонами добавили в `docker-entrypoint.sh` шаг
`composer install` при первом старте; в README
добавили флаг `--build` к команде запуска, чтобы образ гарантированно пересобирался.

Обращений к автору: 0 — проблемы продиагностированы и починены самостоятельно,
без обращения. Оба прогона уложились в требование ≤ 30 минут.

## 7. Учение 2 (перезагрузка)

Команда после `sudo reboot`: `cd ~/WA2-tasks && docker compose up -d && ./smoke.sh`.

Все поднялось само. `docker compose up -d` показал все пять контейнеров
уже в состоянии `Running` (политика `restart: unless-stopped` и healthcheck успели
поднять их сами, ещё до ручной команды). `smoke.sh` сразу отдал «всё зелёное».

**Добавили заранее до перезагрузки**
- `restart: unless-stopped` у всех пяти сервисов (`nginx`, `php`, `api`, `mysql`, `redis`);
- healthcheck у `mysql` и у `redis`;
- `depends_on` с `condition: service_healthy` у `php` и `api`.

**Итоговое время:** 0:15,46 — при требовании ≤ 10 минут.

## 8. Долг

Полная таблица — `TECH_DEBT.md`, 10 пунктов.

**Три самых опасных, на мой взгляд:**

1. **Пароль от MySQL зашит в коде** (`services/api/app/main.py:18`).
2. **Синхронная рассылка письма всем пользователям в цикле** при создании объявления
   (`services/php/app/Http/Controllers/AdController.php:57-63`).
3. **Клик по категории ищет по названию, а не фильтрует** (`feed.blade.php:19` +
   `AdController.php:19-24`).

## 9. Требования

Полная таблица — `docs/requirements.md`.

**Числа, предложенные нами самими:**
- страница ≤ 3 с — порог, после которого пользователи обычно уходят; Виктор сказал
  только «чтобы не крутилось вечно».
- развернуть с нуля ≤ 30 мин, 0 обращений — «если вы пропадёте» в
  измеримое действие.
- найти место по карте ≤ 5 мин — «чтобы разобрался другой человек» в измеримое
  условие для проверки на постороннем.
- подняться после перезагрузки ≤ 10 мин одной командой — «без шаманства».

Отдельно, добавлены две новые строки требований целиком:
секретов в репозитории — 0 (репозиторий передаётся третьим лицам) и воспроизводимость
сборки (без нас разворачивать иначе — лотерея).

## 10. Письмо заказчику

> Виктор, привет. Отчитываюсь по трём твоим вопросам.
>
> **Работает.** Прогнал восемь функций и оба твоих условия (вход через GitHub, база
> отвечает) одним скриптом. Всё зелёное, вывод приложу ниже. Гонять можно после каждой
> нашей правки одной командой.
>
> **Ест мало.** Вся система вместе +- 0,5 ГБ памяти из твоих 4. 
> Диск съедают в основном сами данные — меньше 1 ГБ. В десятку в
> месяц укладываемся с большим запасом.
>
> **Если пропадём.** Развернул проект с нуля на чистой машине по своей же инструкции -
> первая попытка упёрлась в мелочь, так как забыл один шаг сборки, затем поправил
> README, вторая попытка — чуть больше минуты. Перезагрузил сервер целиком и поднялось
> само, без единой команды, за 15 секунд.
>
> **Что кривого — как просил, заранее.** Нашёл несколько мелких мест, все записаны с
> адресом в коде. Хочу два выделить отдельно: во-первых, у нас в коде **прямо прописан пароль от
> базы** — пока репозиторий у нас двоих, риска нет, но перед тем как показывать код
> кому-то ещё, это надо убрать. Во-вторых, **клик по категории в
> объявлениях работает не так, как должен** — сейчас это на деле текстовый поиск
> по названию категории, а не честный фильтр, поэтому можно словить лишние объявления или
> пропустить нужные.
>
> И четыре числа, которые я поставил сам: страница отвечает
> ≤ 3 с · развернуть с нуля ≤ 30 мин · найти нужное место по карте ≤ 5 мин · подняться
> после сбоя ≤ 10 мин одной командой.
>
> Резерва нет, так как ты выбрал один сервер. Значит, если сервер не просто перезагрузится, а
> реально откажет (диск, железо), мы не сможем работать, пока кто-то не поднимет новый и не развернёт
> на нём проект заново.

## 11. Выводы

**Что оказалось неожиданным:**
- стартовый репозиторий не поднимался одной командой уже на первом шаге, при том что по
  методичке ожидалось «почти работает, лишь бы найти дефекты»;
- связка `restart: unless-stopped` + healthcheck сработала настолько хорошо, что после
  реальной перезагрузки хоста контейнеры поднялись сами ещё до того, как руками была
  запущена команда из учения 2;
- очень много дефектов и проблем в коде, которые очень хотелось исправить, но это темы следующий практик.

**Сколько заняла каждая часть работы:** (оценил приблизительно)

| Часть | Время  |
|---|--------|
| Разворачивание и починка стартовых ошибок (блок A) |  1 час |
| `smoke.sh` (блок B) | 1 минута |
| Замеры (блок C) | 10 минут |
| Карта системы (блок D) | 1 час  |
| Записи о решениях, ADR (блок E) | 1 час  |
| Оба учения (блок F) | 10 минут |
| `TECH_DEBT.md` (блок G) | 30 минут |
| `docs/requirements.md` + письмо заказчику (блок H) | 30 минут |
