# Технический долг

Зафиксировано на практике 1 (обзор системы). Ничего из этого не чинится сейчас — только диагностика,
согласно правилу «нашёл — записал, но не чинишь, если дефект не темы недели».

| #  | Что не так | Где (файл:строка) | Чем грозит                                                                                                                                                                                                 | Когда чиним |
|----|---|---|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---|
| 1  | Пароль от MySQL зашит прямо в коде (`password="boardy"`), хотя рядом уже есть модуль, читающий пароль из окружения | `services/api/app/main.py:18` | утечка при передаче репозитория Виктору или третьим лицам; смена пароля требует правки кода, а не конфига                                                                                                  | тема 6 (конфигурация и секреты снаружи) |
| 2  | `get_db()` дублирует уже существующий `get_connection()` из `database.py`, но с захардкоженным паролем вместо `os.getenv` — притом что импорт «правильного» модуля стоит рядом и не используется | `services/api/app/main.py:9,14-21` | правки пароля/хоста в одном месте не долетают до другого — рассинхрон конфигурации                                                                                                                         | тема 2 (чистый код: DRY) |
| 3  | Класс `ConnectionManager` объявлен дважды подряд — второе объявление молча затирает первое | `services/api/app/main.py:24-34` (мёртвый код, перекрыт объявлением на 36-51) | первая версия — мёртвый код, вводит в заблуждение при чтении                                                                                                                                               | тема 2 (чистый код) |
| 4  | Обновление ленты объявлений сделано через `sleep`-поллинг (`time.sleep(2)` в бесконечном цикле) вместо очереди/подписки на события | `services/api/app/main.py:56-67` | не масштабируется на несколько инстансов API, лишняя нагрузка на Redis и БД впустую, задержка до 2 секунд                                                                                                  | тема 9 (очереди и фоновые задачи) |
| 5  | `print("DEBUG ...")` вместо нормального логирования в двух местах | `services/api/app/main.py:64,87` | в проде debug-вывод либо теряется, либо засоряет stdout контейнера                                                                                                                                         | тема 3 (наблюдаемость) |
| 6  | Забытый `var_dump($request->all())` выводит все параметры запроса прямо в HTML на каждой загрузке ленты | `services/php/app/Http/Controllers/AdController.php:18` | утечка данных запроса в разметку страницы; сломанная вёрстка                                                                                                                                               | тема 2 (чистый код) |
| 7  | Искусственная задержка `usleep(300000)` на каждой загрузке ленты — 300 мс без всякой причины | `services/php/app/Http/Controllers/AdController.php:17` | напрямую бьёт по требованию Виктора «страница отвечает не дольше 3 секунд»; искажает замеры быстродействия                                                                                                 | тема 2 (чистый код) |
| 8  | Клик по категории в сайдбаре делает текстовый поиск по названию категории (`/?q=Название`), а не фильтрует объявления по `category_id` — в `AdController::index` вообще нет ветки фильтрации по категории | `services/php/resources/views/feed.blade.php:19` (ссылка) и `services/php/app/Http/Controllers/AdController.php:19-24` (нет фильтра) | функция «категории» фактически не работает так, как ожидает пользователь: похожие по названию объявления из других категорий тоже попадают в выдачу, а объявления без совпадения в названии/описании — нет | тема 2 (чистый код: слои и границы) |
| 9  | Рассылка письма всем пользователям синхронно, в цикле, прямо внутри обработчика создания объявления | `services/php/app/Http/Controllers/AdController.php:57-63` | чем больше пользователей — тем дольше висит запрос `POST /ads`; при падении почтового сервиса объявление всё равно создаётся, но запрос может подвиснуть или упасть после записи в БД                      | тема 9 (очереди и фоновые задачи) |
| 10 | `.env.example` по умолчанию содержит `DB_CONNECTION=sqlite`, хотя вся инфраструктура рассчитана на MySQL — нужна ручная правка при разворачивании | `services/php/.env.example:19` | лишнее ручное действие при разворачивании с нуля; в рабочей копии появлялся посторонний sqlite-файл из-за этого `services/php/boardy`                                                                      | тема 6 (конфигурация снаружи) |

